

// Quem é o maior 



    var valorx = parseInt(prompt("Digite o primeiro valor"));
    var valory = parseInt(prompt("Digite o segundo valor"));
    
    if  (valorx > valory) {
        alert ("Valor X(primeiro) é o maior número: " + valorx);
    }
    
    else {
            alert ("Valor Y(segundo) é o maior número: " + valory);
        }
